[pytest]
testpaths = tests
# web3 6.20.0 registers a pytest11 plugin (web3.tools.pytest_ethereum) that
# fails to import against current eth_typing. We never use it — disable it so
# the suite collects cleanly.
addopts = -p no:pytest_ethereum
